# Test environment configuration
# This file is used only for testing, not for production

# Use mock API keys for testing
GCP_API_KEY=test_api_key_for_unit_tests
GEMINI_MODEL=gemini-2.0-flash-lite

# Logging level for tests
RUST_LOG=info
